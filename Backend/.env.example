# Backend runtime environment variables
# Copy to `.env` and update values for your environment. This file is gitignored.

# Server
PORT=3000

# JWT secret used to sign tokens
JWT_SECRET=change_me_in_production

# Postgres connection (used by app if provided)
# Example: postgres://user:password@host:5432/dbname
DATABASE_URL=

# Or individual PG_* variables (used by scripts)
PGHOST=localhost
PGPORT=5432
PGUSER=fleet
PGPASSWORD=fleetpass
DB_NAME=fleetflow