POSTGRES_USER=fleet
POSTGRES_PASSWORD=fleetpass
POSTGRES_DB=fleetflow

# Backend runtime environment variables
# Copy to `.env` and update values for your environment. This file is gitignored.

# Server
PORT=3001

# JWT secret used to sign tokens
JWT_SECRET=fleetflow_dev_jwt_secret_please_change

# Postgres connection (used by app if provided)
# Example: postgres://fleet:fleetpass@localhost:5432/fleetflow
DATABASE_URL=postgres://fleet:fleetpass@localhost:5432/fleetflow

# Or individual PG_* variables (used by scripts)
PGHOST=localhost
PGPORT=5432
PGUSER=fleet
PGPASSWORD=fleetpass
DB_NAME=fleetflow