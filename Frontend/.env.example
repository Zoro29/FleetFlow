# Frontend environment variables for Vite
# Copy to `.env.development` and/or `.env.production` and update values.

# Base API URL used by the frontend. Must be prefixed with VITE_.
VITE_API_BASE=http://localhost:3000/api